alias rm='rm -i'
alias type='type -a'
alias grep='grep --color=auto'

# application aliases
alias vi='nvim'
alias vim='nvim'
alias ni='NVIM_APPNAME=nvim-legacy nvim'
alias lg='lazygit'

# git aliases
alias g='git'
alias gdiff='git diff --output-indicator-new=" " --output-indicator-old=" "'
alias gp='git push --force-with-lease'
alias gu='git pull'
alias gsw='git switch'
alias gswm='gsw main && gu && clean-branches && git branch'

# npm aliases
alias nd="npm run dev"

# custom scripts aliases
alias cb="clean-branches"
