# Interactive sessions only
case $- in
    *i*) ;;
    *) return ;;
esac

# Shared configs
source "${XDG_CONFIG_HOME:-$HOME/.config}/zsh/aliases"
source "${XDG_CONFIG_HOME:-$HOME/.config}/zsh/less_colors"
source "${XDG_CONFIG_HOME:-$HOME/.config}/zsh/palette.zsh"

# Shell-specific aliases
alias ls='ls -F --color=auto'

# History
HISTSIZE=10000
HISTFILE=~/.bash_history
HISTCONTROL=ignoredups:erasedups
shopt -s histappend

# Completions (Homebrew)
if [ -f /opt/homebrew/etc/bash_completion ]; then
    source /opt/homebrew/etc/bash_completion
fi

# Git branch for prompt
__git_branch() {
    git branch 2>/dev/null | sed -n '/^\* /s/^\* //p'
}

# Prompt: warm-white path + red branch + gold $
# Distinct from zsh (gold path) — path color identifies the shell at a glance
PS1='\[\e[38;2;'"$P_BASE06_RGB"'m\]\w\[\e[0m\] \[\e[38;2;'"$P_BASE08_RGB"'m\]$(__git_branch )\[\e[0m\]\[\e[38;2;'"$P_BASE0A_RGB"'m\]\$\[\e[0m\] '
