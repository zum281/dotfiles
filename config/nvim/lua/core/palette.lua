-- Canonical color palette. All values are moonfly native.
-- Mirror manually in: zshrc, tmux.conf, ghostty/config.
return {
	accent = "#ae81ff", -- moonfly violet
	ok = "#36c692", -- moonfly emerald (success)
	warn = "#cfcfb0", -- moonfly wheat   (warning)
	err = "#ff5454", -- moonfly red     (error)
	fg = "#b2b2b2", -- moonfly foreground
	fg_dim = "#4e4e4e", -- moonfly comment grey
	bg = "#000000", -- background (customized)
}
